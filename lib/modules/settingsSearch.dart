import 'package:flutter/material.dart';

class SettingsSearch extends StatelessWidget {
  const SettingsSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
          child: Text(
              'SettingsSearch'
          )
      ),
    );
  }
}